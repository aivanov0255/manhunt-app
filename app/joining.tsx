import { View, Text } from "react-native";

export default function JoiningScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#6043A9" }}>
            <Text style={{ fontSize: 30, fontFamily: "Jersey", color: "#FFFFFF" }}>Joining Game...</Text>
        </View>
    );
}